- - Для системы уведомления мониторинга сетевой инфраструктуры, необходимо разработать функцию, которая будет выводить список сработавших триггеров для передачи уведомлений в сервис мониторинга 24/7. На вход функция ожидает набор исторических данных для ручек и число, которое означает порог разницы (условие >=) между двумя соседними показаниями в истории для триггера. in = { "health" : { "web/1/handler": [ 1, 2, 2, 4, 4, 4, 4 ], "api/6/handler": [ 0, 0, 0, 0, 1, 3, 0, 1, 0 ], "js/3/handler": [ 0, 0, 0, 1, 0, 0, 0, 0, 0 ] }, "triggerDiff" : 2 } out = ["web/1/handler", "api/6/handler"] \*/ func findTriggers(data map[string][]int, trigger int) []string { // Решение здесь }
- /_ Имеется информация о товарах, которыми торгуют продавцы на торговой площадке. Необходимо для каждого продавца определить уникальные товары, которые представлены только у него. sellers = { 1: ['варежки', 'шуба', 'валенки', 'стол', 'шапка', 'шарф', 'кофта', 'рубашка'], 2: ['шкаф', 'тумба', 'шапка', 'стол', 'ложка', 'кофта'], 3: ['вилка', 'кофта', 'тарелка', 'шарф', 'шуба', 'стол', 'кастрюля'], 4: ['ковер', 'тумба', 'кофта', 'пылесос', 'валенки', 'ложка', 'стол'] } unique_goods = { 1: ['варежки', 'рубашка'], 2: ['шкаф'], 3: ['вилка', 'тарелка', 'кастрюля'], 4: ['ковер', 'пылесос'] } sellers = { 1: ['шкаф', 'тумба', 'шапка', 'стол', 'ложка', 'кофта'] } unique_goods = { 1: ['шкаф', 'тумба', 'шапка', 'стол', 'ложка', 'кофта'] } sellers = { 1: ['шкаф', 'тумба', 'шапка', 'стол', 'ложка', 'кофта'], 2: ['шкаф', 'тумба', 'шапка', 'стол', 'ложка', 'кофта'] } unique_goods = { } _/ func getUniqueGoods(sellers map[int][]string) map[int][]string { //Решение здесь }
- /_ Для системы аналитики поведения пользователей на площадке необходимо определить самые популярные категории товаров, которые просматривал пользователь. На вход подается список категорий, которые посетил пользователь, и число `k`. Необходимо вернуть список из `k` самых часто встречающихся категорий. Если несколько категорий имеют одинаковое количество просмотров, то приоритет отдается той, которая лексикографически (по алфавиту) меньше. categories := []string{"auto", "realty", "auto", "auto", "realty", "jobs", "services"} k := 2 result := []string{"auto", "realty"} // "auto" встречается 3 раза, "realty" — 2 раза. categories := []string{"phones", "laptops", "phones", "laptops"} k := 1 result := []string{"laptops"} // И "phones", и "laptops" встречаются по 2 раза. // По условию при равенстве сортируем по алфавиту: "laptops" идет раньше "phones". categories := []string{"a", "b", "c"} k := 5 result := []string{"a", "b", "c"} // k больше длины списка, возвращаем все, отсортированные по частоте (тут 1) и алфавиту. _/ func getTopKCategories(categories []string, k int) []string { // Решение здесь }
- /_ У курьера доставки есть список временных интервалов, когда он занят развозом заказов. Интервалы могут пересекаться (например, курьер взял два заказа, которые везет попутно). Необходимо объединить все пересекающиеся или соприкасающиеся интервалы в один, чтобы получить чистое время занятости курьера. На вход подается список интервалов, где каждый интервал задан как `[начало, конец]`. Вернуть список объединенных интервалов. // Интервалы: [1, 3] и [2, 6] пересекаются -> становятся [1, 6] // [8, 10] и [15, 18] идут отдельно intervals := [][]int{ {1, 3}, {2, 6}, {8, 10}, {15, 18}, } result := [][]int{ {1, 6}, {8, 10}, {15, 18}, } // Интервалы соприкасаются границами: [1, 4] и [4, 5] -> становятся [1, 5] intervals := [][]int{ {1, 4}, {4, 5}, } result := [][]int{ {1, 5}, } // Интервалы не отсортированы на входе, и один полностью внутри другого intervals := [][]int{ {1, 4}, {0, 4}, } result := [][]int{ {0, 4}, } _/ func mergeIntervals(intervals [][]int) [][]int { // Решение здесь }
- /_ В системе обработки платежей необходимо реализовать модуль быстрого поиска связанных транзакций. Модуль должен найти в истории за день два платежа, сумма которых в точности равна заданному лимиту (target), указывающему на подозрительную активность. in = { "amounts": [100, 500, 250, 750, 1000], "target": 1000 } out = [2, 3] // Индексы транзакций 250 и 750 _/ func findTransactionPair(amounts []int, target int) []int { // Решение здесь }
- /_ Для сервиса управления инфраструктурой (Infrastructure as Code) нужно написать пре-валидатор структуры конфигурации. Конфигурация поставляется как поток символов-ограничителей блоков: (), [], {}. Необходимо проверить, что все блоки закрыты корректно и соблюдена правильная вложенность. in = ["{", "[", "]", "(", ")", "}"] // true in = ["{", "[", "}", "]"] // false _/ func IsConfigStructureValid(tokens []string) bool { // Решение здесь }
- /_ Сборщик логов получает пакет записей с разными уровнями важности: 0 (INFO), 1 (WARNING), 2 (ERROR). Для экономии памяти нужно отсортировать этот пакет in-place (без выделения доп. массивов), чтобы сначала шли все INFO, затем WARNING и в конце ERROR. in = [1, 0, 2, 1, 0] out = [0, 0, 1, 1, 2] // INFO, INFO, WARN, WARN, ERROR _/ func SortLogsBySeverity(logs []int) { // Решение здесь }
- /_ Система защиты от DDoS анализирует последовательность идентификаторов входящих пакетов. Для выявления "разнообразного" трафика нужно найти длину самого длинного фрагмента потока, в котором все пакеты уникальны (не повторяются). in = "aabccbb" out = 3 // Участок "abc" _/ func LongestUniqueTrafficBurst(stream string) int { // Решение здесь (Optimized Sliding Window) }
- /_ Реализуйте простую Event Bus. Система должна позволять: 1. Subscribe(topic string) <-chan string — получить канал для чтения событий темы. 2. Publish(topic string, data string) — отправить событие всем подписчикам темы. Учтите, что подписчиков на одну тему может быть много, и отправка одного события не должна блокировать других подписчиков. _/ type EventBus struct { // Решение }

https://s.vpnreal.io/bHE7TvupMGRCT3Az
